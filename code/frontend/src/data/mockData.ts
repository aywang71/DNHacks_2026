import type { RiskEvent } from '../types'

// Kept as an empty compatibility module for old imports. The application reads
// the validated static GapPair export through data/provider.ts instead.
export const vessels: RiskEvent[] = []
