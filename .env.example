# Store the real value in your user environment, a local .env ignored by Git,
# or a secret manager. Never commit the token.
GFW_API_TOKEN=replace_with_your_personal_gfw_api_token

# Server only. Never use a VITE_ prefix for secrets.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
AI_PORT=3001
